Se instala y configura WireGuard para crear una red VPN hub-and-spoke donde el coordinador actúa como hub (`10.10.10.1`) y cada worker se conecta como spoke con su propia clave.

```bash
sudo apt update
sudo apt install wireguard
sudo apt install ufw
sudo apt install iperf3
```

```bash
# Asegurar que los archivos creados solo sean legibles por el usuario actual
umask 077

# Generar la llave privada y derivar la llave pública
wg genkey | tee privatekey | wg pubkey > publickey
```

```bash
curl -4 icanhazip.com
```

```bash
sudo nano /etc/wireguard/wg0.conf
```

```bash
[Interface]
PrivateKey = 4[PEER_PRIVATEKEY]
Address = 10.10.10.2/24
DNS = 1.1.1.1
ListenPort = 51820

[Peer]
PublicKey = [HUB_PUBLICKEY]
Endpoint = [IP_HUB]:51820
AllowedIPs = 10.10.10.0/24
PersistentKeepalive = 25
```

```bash
sudo wg-quick up wg0
```

```bash
sudo wg showconf wg0
```

```bash
ping 10.10.10.1
```

```bash
sudo wg-quick down wg0
```

