# Network configuration
COORDINATOR_URL=http://coordinator:8080

# Fractal image dimensions
IMAGE_WIDTH=3840
IMAGE_HEIGHT=2160

# Task partitioning — rows per worker task
BLOCK_SIZE=100

# Mandelbrot computation — max iterations per pixel
MAX_ITERS=1000

# Mandelbrot viewport (worker only)
X_MIN=-2.0
X_MAX=1.0
Y_MIN=-1.5
Y_MAX=1.5
